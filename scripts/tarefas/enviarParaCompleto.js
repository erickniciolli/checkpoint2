function enviarParaCompleto () {
    let tarefa = document.getElementById(tarefa.id);
    

    let adicionandoPendente.innerHTML=`<img id="completar" src="/checkpoint2/assets/voltarPendente.png" onclick="alterarStatus(${tarefa.id}, ${tarefa.description}, true)">`

    tarefa.removeChild[4]
    tarefa.appendChild(adicionandoPendente)

// peguei a tarefa conforme o tarefa.id, adicionei a imagem para enviar para pendente, e removi a imagem para enviar para concluído


// abaixo pensei em chamar a função tarefasTerminas.js, será que funciona??
// tarefasTerminadas(tarefa)

}